import './<%= name %>.css';
import template from './<%= name %>.tmpl.html';
import controller from './<%= name %>.controller';

const <%= name %>Component = {
  bindings: {},
  require: {},
  template,
  controller
};

export default <%= name %>Component;
